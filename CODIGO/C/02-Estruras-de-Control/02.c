/*
    Escribe un programa que imprima los numeros del 1 al 10 usando u bucle for
*/

#include <stdio.h>

int main () {
    printf("Aqui se muestran los numeros del 1 al 10, dentro de un bucle for\n");

    for (int i = 1; i <= 10; i++)
    {
        printf("%d\n", i);
    }
    
    return 0;
}